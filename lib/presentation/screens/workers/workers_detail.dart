import 'package:flutter/material.dart';

class WorkersDetail extends StatelessWidget {
  const WorkersDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
